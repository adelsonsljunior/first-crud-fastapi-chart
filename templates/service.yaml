apiVersion: v1
kind: Service
metadata:
  name: {{ include "first-crud-fastapi-chart.fullname" . }}
  labels:
    {{- include "first-crud-fastapi-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: api
      protocol: TCP
      name: api
  selector:
    {{- include "first-crud-fastapi-chart.selectorLabels" . | nindent 4 }}
