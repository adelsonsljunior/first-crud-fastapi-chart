apiVersion: v1
kind: Service
metadata:
  name: {{ include "first-crud-fastapi.fullname" . }}
  labels:
    {{- include "first-crud-fastapi.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: api
      protocol: TCP
      name: api
  selector:
    {{- include "first-crud-fastapi.selectorLabels" . | nindent 4 }}
