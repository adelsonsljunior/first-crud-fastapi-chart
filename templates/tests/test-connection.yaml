apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "first-crud-fastapi-chart.fullname" . }}-test-connection"
  labels:
    {{- include "first-crud-fastapi-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "first-crud-fastapi-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
